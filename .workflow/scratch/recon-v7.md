# v7 recon digest (scouted 2026-08-23, file:line refs verified by scouts)

## Universe / galaxy (Plan A1)
- UniverseWorkspace.tsx (src/features/universe/) top component, data-testid="universe-workspace"; DetailPanel.tsx; repertoire.ts pure model layer; format.ts (formatDuration/formatSince); universe.css (header comment line 6: "NO ambient animation" — v7 deliberately retires this rule for the galaxy, with prefers-reduced-motion honored).
- Route: Shell.tsx:90 {id:"universe"}, render at Shell.tsx:1040-1047, lazy at :214. Deep link: onOpenPractice(piece: PracticePieceContext|null) → Shell.tsx:911-917 openFromUniverse → openPiece. PracticePieceContext={piece_id:number; title:string} (types.ts:75-78).
- Data: universeSnapshot() api.ts:4-6 → tauri cmd "universe_snapshot" → src-tauri/src/universe.rs snapshot() line 140, pure aggregate() line 177. Only rep_open|rep|verdict|tempo_change events count (PRACTICE_EVENT_KINDS line 17).
- Types (src/features/universe/types.ts): UniverseSnapshot{generated_at,definitions,traces,totals,pieces} (67-73); UniversePiece (48-65): focused_seconds, active_days_28, regions_total/practiced/revisited, mastered_targets?, recovered_targets?, open_recovery_debt?, practice_sessions?, earned_maturity?, quality_brightness, last_practiced, region_signals[]. RegionSignal (27-46): focused_seconds, active_days_28, practiced, revisited, quality_brightness, last_practiced, practice_events, rated_rep_events, clean_rep_events, distinct_practice_dates, mastery_contracts_completed?, recovery_resets?, recovered?, open_recovery_debt?, practice_sessions?.
- Mastery: blockState() repertoire.ts:51-57 (recovering > mastered(mastery_contracts_completed>0) > revisited > practiced > untouched). Backend: universe.rs:228-233 SetEvidence mastery_verified && mastery_satisfied; piece mastered_targets universe.rs:279-284.
- earned_maturity (0-1 composite, universe.rs:413-442: 0.25 log focused + 0.20 continuity + 0.20 coverage + 0.25 mastery + 0.10 recovery) and quality_brightness (0.92-1.00, universe.rs:485-490) are computed but UNUSED by current UI — intended galaxy drivers.
- focused time: metrics::focused_seconds(events) src-tauri/src/metrics/mod.rs:32-49, gap-sum ≤ IDLE_THRESHOLD_SECS=120.
- NO JS motion utility / rAF anywhere; app-wide motion pattern is CSS-only with @media (prefers-reduced-motion: reduce) blocks (~16 css files, e.g. Popover.css:36, App.css:151). No JS matchMedia reads exist.
- Tests: UniverseWorkspace.test.tsx (line 204 asserts "no simulation, no animation frame loop, no keyframes" — REPLACE deliberately; line 226 deterministic layout order), DetailPanel.test.tsx, repertoire.test.ts, api.test.ts; Rust inline tests universe.rs:565-1049.

## Calendar / Today / day-close / streak (Plan A2-A3)
- CalendarWorkspace.tsx: today/weekStart/weekEnd :41-59; plannedVsDone Record<string,PlannedVsDoneDay> :56-58; weekMaxMinutes :60-74; day-cell bars at :451 (.calendar-day-progress-bars). plannedVsDone.ts: mergePlannedVsDone(sheets, days, weekDates); PlannedVsDoneDay={plannedMinutes,plannedItems,doneMinutes,attempts}, doneMinutes=round(focused_seconds/60).
- Rust read model: store/history_days.rs:33-43 HistoryDaySummary{date,focused_seconds,session_count,attempts,cleans,sets_touched,mastered_sets,pieces}; day_candidates :114+ uses date(...,'localtime') — THE local-day convention.
- metrics::streak() metrics/mod.rs:54-75 EXISTS (pure consecutive-local-days), currently only used by piece-scoped progress_summary (:168-191). A2 needs a new GLOBAL day-level source.
- Day close: SessionBar.tsx:58 confirmingEndDay, :117-149 inline-confirm End-my-day (window.confirm BANNED). Rust: sessions/mod.rs — session_last_event_and_same_local_day (:163,:328,:401); rollover_close :223 (+RolloverPauseHook :51-52); end_raw_locked :394 → end_and_export :430 (takes pieces_dir).
- Settings: src-tauri/src/settings.rs SettingsSnapshot :57-78 / SettingsPatch :80-100; storage = generic `setting` key/value table (precedent: calendar.daily_capacity_minutes, calendar.rs:18-20,140-150). Frontend: SettingsPanel.tsx (numeric-field pattern :596-600).
- App-data files: lib.rs:2086-2088 app_data_dir()/codakiller.db. Bounded file-cache precedent: score/page_cache.rs (index.json + numbered files, caps). Day photos = DURABLE → app_data_dir()/day-photos/ new subdir.

## Dock panels / streaming / devMock (Plan B3)
- Active framework = src/features/dock/ (NOT the legacy src/components/FloatingPanel.tsx). DockProvider.tsx ensurePanel(id,defaultPosition,width) :117; useDock(id) :282 (open/close/minimize/flash/isOpen/isMinimized). Panel pattern: <DockPanel id title width defaultPosition> (RepPanel.tsx:143-156, ClockPanel.tsx:222-228).
- dockState.ts: clampPosition :94-111 (MIN_VISIBLE_PX=48); MIN_VISIBLE_ON_OPEN_PX=160 :149; clampPanelWidth :58-67 (MIN_PANEL_WIDTH=200, edge 24); dockPanelMaxHeight :201-211; pills portal to DockPillBar (DockPanel.tsx:232-280, PILL_STACK_STEP_PX=44); DOCK_Z_BASE=10 :379, DOCK_Z_CEILING=39 :394, dockPanelZIndex :405-412; NAV_RAIL_WIDTH=148 :280; DENSE_LAYOUT_FLOOR={720,520} :286 — every default position must fit; DOCK_CHAIN_BASE_Y=16 :316 anchors PausedSetsTray→ClockPanel chain. New panel = own DEFAULT_POSITION + PANEL_WIDTH extending the chain, tested at 720x520.
- Rust→frontend streaming = tauri named events via app.emit: metronome.rs:773-775 emit("metro://state",s); frontend listen-then-fetch-once pattern useMetronome.ts:194-221 (listen FIRST, then one invoke("metro_state") — never polls). Other events: rep://state (rep/mod.rs:1308), session://event (sessions/mod.rs:300), voice://tts (lib.rs:2157), score://navigate (voice_loop.rs:984). No Tauri Channel<T> usage anywhere. Dynamics meter → new "dynamics://level" emit-per-update from audio thread.
- devMock: src/devMock/tauriDevMock.ts (~3100 lines, single routeCommand switch; grep -a needed). BINDING RULE (NOTES.md:32): devMock must cover every command the frontend calls on a timer; sibling test tauriDevMock.<name>.test.ts via window.__TAURI_INTERNALS__.invoke. Audit tool .workflow/devmock-coverage-audit.mjs.
- Ack policy: routine confirmations → Confirm::chime() (audio/chime.rs ack_chime :65); info-carrying → Confirm::say() (voice_loop.rs:260-330, tests :1512,:2502,:2638). ClockPanel playChime (/chime.wav, ClockPanel.tsx:158-167) is UI-local, separate.

## Rust audio / STT / migrations (Foundations, Plan B1)
- migrations.rs: SCHEMA_VERSION=14 :11; SCHEMA_V13 :945, SCHEMA_V14 :974 (additive template; note :962-973); migrate() :1442 cascading `if version < N { BEGIN IMMEDIATE; execute_batch; PRAGMA user_version=N; COMMIT }`, v13→v14 step :1675-1687 = template for v15.
- Rehearsal test: store/mod.rs:1675 rehearse_migration_on_real_database_copy #[ignore], env CODAKILLER_MIGRATION_COPY, refuses paths containing com.christian.codakiller (:1684-1692), asserts row-count invariance on preserved_tables = region, rep_block, rep, goal, session, session_event (:1694-1705).
- session.focused_seconds: added SCHEMA_V3 :201 (nullable, no default). NOTHING writes it (verified: all INSERT INTO session omit it; no UPDATE anywhere). All reads go through metrics::focused_seconds. Column is dead → safe to DROP (SQLite ALTER TABLE DROP COLUMN; check bundled SQLite ≥3.35).
- audio/mod.rs: OUTPUT-ONLY engine — Engine::start :280 spawns codakiller-audio thread, build_output_stream::<f32> :402-458, choose_f32_config :465; realtime discipline doc :1-42 (callback owns ClickClock+Mixer; inputs = lock-free ArrayQueue<ClickPattern> + AtomicU32 gain; TTS PCM via ArrayQueue<Vec<f32>>). NO input stream exists anywhere.
- metronome.rs = control wrapper (metro_start/stop/set/state commands, mutex-guarded over lock-free engine :22-24).
- STT: stt/mod.rs re-exports supervisor (stt/supervisor.rs, NOT read in depth — follow up if touching STT). SttSupervisor::spawn_with_config launches vendored `hear` child process (own process group), stdout lines → sink. Half-duplex: SttHandle::set_gate(bool) drops lines when closed (stt/mod.rs:7-10); gate calls voice_loop.rs:336,359,419,3074; ReopenGuard :407; spawn site voice_loop.rs:1246; resolve_hear_bin lib.rs:328.
- Commands: #[tauri::command] fns mostly in lib.rs (or domain module like metronome.rs), registered in generate_handler! at lib.rs:2240. New store modules under src/store/ (crud.rs, calendar.rs, measure_map.rs... pattern), mod-declared in store/mod.rs.
- Locks: Store{conn: Mutex<Connection>} store/mod.rs:56-57; documented order `active` < `store.conn` (rep/mod.rs:817-819); lattice law from NOTES: lifecycle < current < pause_hook < active < store.conn; NO path holds rep.active across a SessionService call; early-outs precede session resolution.

## Brain/Assistant (Plan C)
- Providers: Claude primary (api.anthropic.com/v1/messages, model claude-sonnet-4-6), Gemini fallback (generativelanguage, gemini-flash-latest). Keys: macOS Keychain service "codakiller", accounts "claude"/"gemini"; keys.rs:59-79 via security find-generic-password. NOTE B67: no Anthropic key present on this machine — Claude path never exercised.
- Request building: brain/provider.rs claude_request :540, gemini_request :562 (system policy + user prompt + grounded JSON context). max_tokens 900 text / 4096 vision; 45s timeout. NO streaming (blocking POST, single JSON).
- Honest-grounding: brain/mod.rs:1312-1586 output_policy_violation_reason() — rejects app-control claims, sensory claims, sensitive phrases; violation → BrainError::PolicyViolation → offline answer substituted.
- Tool scaffolding exists (voice-only, closed schema): ProposedActionBody enum mod.rs:193-205 (Verdict{verdict,note}, Tempo{bpm}, Undo, Restart{required_clean_streak}); ProposedActionInput::validate provider.rs:718-783 (deny_unknown_fields); voice questions only (mod.rs:931-934), typed questions discard proposed_action.
- Confirm-gate idiom: answer carries proposed_action → UI confirm card; PendingIntakeReviews ledger mod.rs:278-322 (one-time, 15-min TTL); apply_intake_review mod.rs:1197-1296 validates answer_id + allowed fields.
- Book corpus: brain/corpus.rs:240-468 — BM25+TF-IDF, heading bonus, max 2 hits/source, chunked by heading/paragraph, cached lexical index, 12MB total/5MB per book. Books: roskell-complete-pianist, gebrian-learn-faster, breth-effective-practicing, gieseking-leimer-technique (markdown files).
- Read models available as tools: history_days(from,to) + history_day_detail(date) store/history_days.rs:299-350; BlockHistory store/model.rs:144-183 (attempts/tries/current+best clean streak/mastery_status/contract fields); store.rep_blocks_for_piece(piece_id); metrics progress_summary (piece-scoped, incl streak); pass_seconds set_contract via practice_v2.rs:1613-1627 + open_set_in_tx :947-960. No carry_forward_seconds field exists in v6 (session planned_seconds in session_plan.rs:289-292).
- Piece metadata (PieceDetail): id,title,composer,folder_path,xml_path,pdf_path,goals[],deadline,target_tempo,hard_spots,current_state,notes,banner_text (v14, ≤140 chars, crud.rs:2820); client context edition_id/edition_label (untrusted, ≤500 chars); brain context.rs:208-219 ui_score_location{surface,current_page,edition_id,edition_label,today_plan}.

## Ship-time facts (checked 2026-08-23)
- Keychain (service `codakiller`): **gemini key PRESENT, claude key ABSENT** (confirms B67).
  Consequences: ledger item 26 (B75 — run measure mapping once on a real score for a live
  datapoint) IS achievable at ship QA via the Gemini vision path; Plan C's live acceptance
  question set also runs on Gemini. The Claude vision path still cannot be exercised at all.
- Release path: `scripts/(C) release-macos.sh` — 8-step gate; step 1 asserts version agreement
  across package.json + src-tauri/Cargo.toml + src-tauri/tauri.conf.json (all three must be
  bumped to 7.0.0 together, currently 6.0.1); then npm test, npm run build, cargo test, clippy,
  bundle, sign, DMG + sha256 into releases/v7.0.0/, staged install to /Applications with
  .CodaKiller.previous.app backup.
- True v7 baseline on main (b39df48, verified post-merge): vitest **2132 passed / 1 skipped
  (172 files)**, cargo **864 lib + 37 across 12 further suites, 0 failed**, tsc clean.
  NOTE: the earlier 2125 figure predates adopting 3 orphaned devMock tests (+7).
