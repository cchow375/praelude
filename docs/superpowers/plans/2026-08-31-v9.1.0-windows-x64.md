# v9.1.0 — Windows x64 Port and Sendable Installer Plan

> Status: **PACKAGE COMPLETE — native Windows 10/11 acceptance pending**
> Date: 2026-08-31
> Version/schema: 9.1.0 / 21
> Installed Mac boundary: 8.2.1 / 20
> Base: packaged, uninstalled Mac v9.0.0/schema-21 Pieces/share-clean candidate

## Goal

Produce one blank, share-clean, current-user x64 NSIS Setup EXE that a pianist can install on
Windows 10 or 11 and use for Pieces/PDF organization and keyboard/mouse practice. This is the same
generic product, never a personal friend fork.

## Binding scope

### Supported in the first Windows port

- blank app-owned first launch and schema-21 local database;
- Pieces Library, nested logical folders, Unfiled and Active/Completed/Archived actions;
- direct local-PDF picker/drop intake and optional IMSLP browser handoff;
- Score rendering and platform-correct score URLs;
- keyboard/mouse Clean, Sloppy, Again, Undo and set/session controls;
- native metronome/chimes, History, Calendar and local persistence;
- Windows-safe file/folder/path/cache behavior; and
- current-user x64 NSIS packaging with WebView2 bootstrap behavior.

### Explicitly unavailable

- `hear`/native STT and Mic controls;
- hands-free voice commands;
- Listen Back and its microphone ownership/capture lease;
- macOS `say` and macOS system-volume boost;
- Windows on Arm, automatic updates or warning-free signed distribution.

No Windows code-signing certificate exists. The artifact is expected to be unsigned; SmartScreen
is an acceptance step, not a condition to disguise.

## Work phases

### 1. Portability boundary

- Replace hard-coded macOS score URL, browser/file opener, picker, home/download and reveal paths
  with target-aware implementations.
- Keep macOS STT behavior intact; compile a Windows unsupported supervisor that reports the
  platform boundary without spawning `hear`.
- Hide/disable Mic, voice, volume and Listen Back surfaces on Windows with truthful copy.
- Sanitize Windows-invalid/reserved piece folder names and normalize trash/path projections.
- Make page-cache replacement and durability behavior safe on Windows.

### 2. Windows package boundary

- Use `src-tauri/tauri.windows.conf.json` to target NSIS/current-user x64.
- Exclude the Mach-O `vendor/bin/hear` resource.
- Run the official Tauri macOS→Windows cross-build with the rustup stable Windows target,
  `cargo-xwin`, LLVM and NSIS. Cross-target check and strict all-target Clippy must pass; do not
  describe them as native Windows tests.
- Stage exactly one versioned Setup EXE, a basename-only SHA-256 sidecar and a build-status record.
- Run the Windows share-clean inspection and record Authenticode honestly.

### 3. Artifact and recipient acceptance

- Record exact filename, size, SHA-256, Authenticode status, runner and run URL/ID.
- Install on clean Windows 10 and Windows 11 x64.
- Verify SmartScreen, current-user install, blank first launch, first PDF, Score, folders/actions,
  a keyboard/mouse practice set, restart persistence and uninstall/reinstall.
- Confirm Mic/voice/Listen Back never present themselves as working.

### 4. Release truth

- Update QA/version/living docs with measured results only. Package source is frozen at
  `2d33004888a97c3ebcb4b7799bf54029efd15626`.
- Tag/push only after the package boundary and pending native-Windows limits are factual in docs.
- Do not call the installer recipient-accepted until clean Windows 10 and 11 evidence exists.
- Do not change the packaged Mac v9.0.0 facts or claim that v9.1 is installed on Christian's Mac.

## Completed package evidence

- Frontend: **PASS — 205 files with 1 skipped / 2,540 tests with 1 skipped**.
- TypeScript and production frontend build: **PASS**.
- Mac-host native tests: **PASS — 1,111 passed / 19 ignored / 0 failed**.
- Mac Rust format and strict Clippy: **PASS**.
- Windows-target `cargo-xwin check` and strict all-target Clippy: **PASS**.
- Native Windows `cargo test`: **NOT RUN**; cross-compilation is not native execution.
- Official Tauri local macOS→Windows NSIS cross-build: **PASS**.
- Recursive frontend/extracted-NSIS blank/share-clean scanner: **PASS**.
- Embedded application identity: **PASS — PE32+ x86-64**.
- Package source commit: `2d33004888a97c3ebcb4b7799bf54029efd15626`.
- Final installer:
  `releases/v9.1.0/windows/CodaKiller-9.1.0-Windows-x64-Setup.exe`.
- Built UTC: `2026-08-31T19:31:59Z`.
- Exact bytes: **7,654,002**.
- SHA-256: `e2e2f3ae8846ef7aca6a6c04b2e1a2f346e97640f5d9089e6e49012365dc5dd4`.
- Signing: **none**; an unsigned SmartScreen warning is expected. Native Authenticode and
  SmartScreen observation remain PENDING.
- GitHub workflow: **REMOVED** because the current OAuth token lacked workflow scope; that
  permission was not bypassed. `scripts/package-windows-cross.sh` is canonical.
- Clean Windows 10 and Windows 11 acceptance: **PENDING**.

## Package privacy boundary

The recursive scanner found no personal files, database, scores, Pieces Library, practice
history, macOS `hear` binary or copyrighted Knowledge/Resources pedagogy payload.
It found no personal or unremapped host paths; remapped `/build-user` paths intentionally remain. The
application still carries inert historical schema/migration metadata and the existing
`com.christian.codakiller` bundle identifier to support compatible upgrades. Those are deliberate
compatibility facts, not bundled user content; Windows upgrade and data preservation have **not**
been exercised.

## Packaging root cause recorded

The first NSIS packaging attempt ended with a misleading `makensis` `std::bad_alloc` while the
process inherited `LC_ALL=C`. Re-running the package step with `C.UTF-8` succeeded. The failure
was locale-driven, not evidence of exhausted memory or corrupt payload. The canonical local
packager now validates its Rust/LLVM/NSIS/7-Zip inputs and never mutates the toolchain or installs
the application.

## Stop conditions

- Any personal DB, score, recording, photo, quote/method/Knowledge payload or `hear` binary enters
  the installer.
- The app renders Mic/Listen Back as usable on Windows.
- Cross-target check, strict Clippy, build or share-clean inspection fails.
- The staged artifact name/version differs from 9.1.0 x64 Setup.
- Documentation fills any artifact/acceptance fact from expectation rather than measurement.

## Next steps

1. Copy the exact hashed Setup EXE to clean Windows 10 and Windows 11 x64 systems.
2. Record install/relaunch, first-run blank state, picker/PDF, folders/actions,
   keyboard/mouse practice, audio, persistence, Authenticode/SmartScreen and uninstall behavior.
3. Keep voice, Listen Back, system TTS and volume boost explicitly unavailable on Windows.
4. Add trusted Authenticode signing only after a real signing identity is available; until then,
   keep the unsigned SmartScreen handoff explicit.
