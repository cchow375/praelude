# v8.1.0 — P3–P6 Completion and Motivation Correction

> **Status: v8.1.0 / schema 19 SHIPPED + INSTALLED 2026-08-27.** The combined remaining Aug 8
> train passed full gates and live migration. Final installed-native Universe and Warmups both
> passed at 720×520 after Warmups moved compact detection to Tauri native logical window bounds.
> Source/artifact facts are frozen; final docs commit, tag and push remain bookkeeping.

## Goal

Finish every remaining item in Christian's Aug 8 practice list as one coherent native-app
release, correct the two v7.2 convenience gaps and the micro-target retry edge, and replace the
failed Universe metaphor with visible earned progress. Preserve the founding law: the pianist
judges the piano; CodaKiller records only explicit human verdicts and deterministic evidence.

## Scope decisions

- The correct denominator is **23 asks**, not 21: A1–A7, B1–B5, C1–C3, D1–D2, E1–E6.
- Assistant cleanup means **OFF, hidden and Rust-gated**. Its off-state Settings guide teaches
  only practice commands, Books/provider furniture is hidden, settle timing remains under Voice,
  and the enable switch stays discoverable. The held Assistant overhaul is a separate project and
  is not merged.
- Pieces use **reversible archive + recent-first**. Folders were declined for this train.
- Warmup visuals are SVG keyboard figures + text, not engraved notation.
- Unkept Listen Back bytes are temporary; only explicit Keep persists.
- Sloppy alone drives tempo demotion. Space / Right Shift / Return remain remappable defaults.
- B5 source activation is in scope; claiming a successful real map without a configured provider
  and live row is not.

## Source-complete slices

### P3 — Voice reliability

- Live-set-only partial fast path for `mark done`, `rep done`, `mark sloppy`, `mark again`;
  routed-intent tail suppression prevents the settled duplicate without swallowing revised speech.
- Short verdict acknowledgement chime; persistent last-three final transcript feed, including
  ignored speech; live 300–2000 ms settle setting.
- Counted deterministic add-Clean/append-only undo grammar and receipts, with no partial mutation
  on over-undo.
- Saved verdict-hotkey remaps publish to the already-mounted HUD.
- Full narrated finals + partial-stream corpus remains a mandatory zero-false-mutation gate.

### P4 — Score map

- Schema 17 `piece_movement`; title/first-page CRUD in Piece Detail; Whole score / movement scope
  in Score for page browsing and anchored sections. No PDF/history rewrite and no hardcoded
  Beethoven seed.
- Measure mapping exposes prerequisites and Claude/Gemini readiness, describes whole-edition
  upload, refuses Start with no key and keeps the review/Apply write gate.
- External gap remains: no Anthropic key and zero live `measure_map` rows at last audit.

### P5 — Warmups

- Search/filter visual catalog (118 code-owned entries at this source boundary), paged for the
  720×520 floor, with keyboard figures and exact how-to text. Layout children explicitly shrink
  inside the effective 480px stage.
- Corrective native QA: at the compact floor Warmups owns a reversible Rep Counter tuck that
  preserves the active set and yields to manual restore/close.
- Saved named/reordered routines with per-item BPM and clean streak.
- Schema 18 routines + hidden reserved system piece `id=0`; exact rep-engine sets and authoritative
  verified-mastery advancement; one completion moment per finished routine.
- System work contributes real focus/streak/technique evidence but never becomes repertoire.

### P6 — Review & flow

- Schema 19 Listen Back: native capture lease physically suspends STT before WebView recording;
  pending/failure firewalls verdicts; teardown restores exact user mute; temporary recordings
  discard unless Keep is selected; kept exact-attempt files are confined/recoverable and replay
  with output-only gain.
- Rotation dock: 2+ exact targets, station duration, ordered/shuffled cycle, chime + explicit Next
  prompt, exact-owned-block pause, exact returned-target validation and one full-cycle completion.
  At 720×520 an unsafe restored/collision `y=360` clamps to `y=164`, leaving 56px clear for Tools.
- Reversible archive/restore, recent-first active/archived groups, distinct **Delete files…**.
- Editable **Sound target** promoted beside the live verdict work.

### Motivation correction

- Remove the decorative galaxy. Universe becomes a canonical evidence dashboard.
- 1 Practice XP = 1 completed focused minute; deterministic level curve.
- Six transparent badge tracks: active days, best streak, focused hours, revisited targets,
  verified mastery and honest recovery; one exact next milestone/rail per track.
- Zero-filled 28-day cadence, current progress rails, active/archived repertoire evidence and a
  separate technique aggregate. Archived and file-deleted lifetime history remains real.
- Warmup routine, full rotation cycle and explicitly kept reference take join earned completion
  moments. Quality/verdict labels never award XP; no coins/social/random/UI-grantable progress.
- Universe tucks (does not end) the floating Rep Counter so progress is visible at every size.

### Corrective edges

- B88: per-set demotion inherit/off/local thresholds + running-HUD subdivision 1–16.
- B89: durable spot `command_id`, payload fingerprint, replayed original receipt, conflict
  rejection and one bounded exact-identity frontend retry.
- Listen Back file reads use compact base64 IPC; startup recovery quarantines unverifiable files,
  rejects symlinks/path escape and keeps delete failures retryable.

## Data and migration truth

Schema sequence is 16→17 movements/archive→18 warmups/system piece→19 rep replay. A disposable
SQLite `.backup` of the live database rehearsed to 19 with integrity OK and the source relationship
graph preserved. It gained only the designed hidden `Warm-ups` system row; new tables began empty.
That rehearsal does not replace installed before/after verification. The fresh preinstall backup
and outgoing rollback below are now prepared separately.

## Release gates

- [x] Final full freeze: frontend 2,650 passed / 1 skipped / 0 failed (209 files passed / 1
      skipped) and TypeScript clean.
- [x] All-target/all-feature native: 1,100 passed / 19 ignored / 0 failed; format and strict
      Clippy clean.
- [x] Five narrated corpora at zero false mutations.
- [x] Production build and all eight macOS release-script gates.
- [x] Browser/devMock evidence for Universe, Warmups, Rotation, Listen Back and movements at the
      recorded viewports, clearly labelled as non-native in `docs/qa/v8.1.0/README.md`.
- [x] Outgoing signed v7.2 rollback archive + SHA and fresh integrity-OK schema-16 live DB backup +
      SHA. Snapshot held **1 open session**, intentionally preserved through install.
- [x] Installed v8.1.0 bundle: plist short/build 8.1.0, bundle id
      `com.christian.codakiller`, strict ad-hoc signature PASS, CDHash
      `8655e9a45d83b7bb56e83a9d14bb477da7da39a9`; not Developer ID signed/not notarized. Final DMG
      10,892,054 bytes, SHA-256
      `e828b861b31d771fde66cd66d48987eb66110f0fd455306a6a12c2f80eb0a271`; fresh launch PASS.
- [x] Live schema 19 integrity OK / zero FK violations; 11 pieces solely due hidden id0 Warm-ups,
      while 230 blocks / 2,034 reps / 46 sessions / 7,873 events / 1 open session stayed exact.
- [x] Final source HEAD and rebuilt exact facts recorded:
      `1a1e38bb7a3757cf90ee6ea814e93d5971c595d6`.
- [ ] Final docs commit, `v8.1.0` tag and private-remote push.

## Acceptance that automation cannot close

- [x] Installed-native Universe at 720×520.
- [x] Rebuilt installed-native Warmups at 720×520: no overlay/clipping, Restore Rep Counter
      exposed in Tools and active set unchanged. Remaining native interaction verdicts stay open.
- [ ] Listen Back permission/device ownership and useful playback at the Steinway.
- [ ] Voice commands over the Steinway, including intended verdict latency and firewall behavior.
- [ ] One real current-edition measure mapping applied to the live DB; Anthropic remains untried
      until a key exists.
- [ ] Sustained-use verdict on whether XP/levels/badges/cadence actually motivate.

## Next steps

Finalize the docs commit, tag and push. Then obtain the real WKWebView microphone/Listen Back and
voice-over-Steinway verdicts, one live-provider mapping and the sustained motivation verdict. The
23-ask boundary is **22 delivered / 1 B5 external-only / 0 absent**; C2 source is delivered but its
hardware acceptance remains external.
