# v3 Parity Inventory — what the rewrite must not silently drop

> **Status:** Historical v3.0 audit artifact. It is not the current capability registry; use the
> Aug 8 rev-3 matrix and v8.1 completion plan for present status and acceptance gaps.

> Source of truth for Phase 8's audit. Every row must be reachable in the v3 frontend or carry an explicit **[approved removal]**. Derived from the v2.0.0 frontend (`src/features/*`) + the backend IPC contract (`src-tauri/src/lib.rs` handler registry). Status column filled during Phase 8.

**Audit run: 2026-07-16 (Task 8.1, Opus).** Method: adversarial — every capability located by grep/read against the CURRENT mounted tree (`src/shell/Shell.tsx` → `App.tsx`), not trusted from phase reports. A component that merely EXISTS is not parity; it must be reachable from a mounted workspace. Legend: ✅ reachable (component / mount site) · ❌ gap (what is missing) · **[approved removal]** (recorded decision cited).

## A. Workspaces (top-level nav)

| Workspace | Main component                            | Must reach parity on                                                                                                  | v3 status                                                                                                                                                                                                                                                                                                                                                     |
| --------- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Today     | `features/today/TodayWorkspace.tsx`       | Session Composer, session-plan review/start, retention queue, active-block HUD, receipts                              | ✅ TodayWorkspace mounted directly in Shell.tsx:437. Composer (SessionComposer @185), Retention (RetentionQueue @261), HUD (RepHud at Shell level, `activeBlock` passed), receipts (ReceiptCenterProvider App.tsx:45), `session_plan_start` via useSessionPlan.                                                                                               |
| Score     | `features/score/ScoreView.tsx` (+ atlas)  | PDF view, region overlays, annotations, **mapping wizard (new)**                                                      | ✅ ScoreWorkspace→ScoreView lazy in Shell.tsx:75. PdfPage, RegionOverlay (annotations), RegionEditor, TutorialPanel, and the **MapScoreWizard** (ScoreView:1935) all mounted.                                                                                                                                                                                 |
| Brain     | `features/brain/BrainWorkspace.tsx`       | typed Q&A, per-piece thread memory, intake-apply proposals, work suggestions, **status line + test-connection (new)** | ✅ BrainWorkspace lazy in Shell.tsx:80. brain_ask/thread_resume/thread_clear/intake_apply/plan_preview + **brain_status** status line. **NOTE:** BrainWorkspace is mounted WITHOUT `onProposedAction` — the wake-cue Brain-proposed verdict/tempo/undo/restart confirm card is not wired to the tab Brain (see §B Voice) — **[approved removal / deferred]**. |
| Ledger    | `features/ledger/LedgerWorkspace.tsx`     | piece→block→rep history drill-in, anomaly panel, inline edit                                                          | ✅ LedgerCalendarWorkspace→LedgerWorkspace lazy in Shell.tsx:87. HistoryPanel drill-in (reps_for_block/progress_summary), AnomaliesPanel (anomalies_list), BlockRow inline label edit (block_update).                                                                                                                                                         |
| Calendar  | `features/calendar/CalendarWorkspace.tsx` | week daily-work, recovery review, capacity                                                                            | ✅ Folded into LedgerCalendarWorkspace behind the in-workspace switch (one nav slot, NOTES Phase 6 decision). daily_work_*, recovery_preview/apply, calendar_capacity_set, RetentionQueue.                                                                                                                                                                    |
| Universe  | `features/universe/UniverseWorkspace.tsx` | earned-signal graph → **draggable force graph + detail panel (new)**                                                  | ✅ UniverseWorkspace lazy + mounted with nav props in Shell.tsx:96/447. universe_snapshot → d3-force draggable graph + detail panel (openPractice/openLedger jumps).                                                                                                                                                                                          |

## B. Cross-cutting features (shared panels)

| Feature     | Component                                                                                                    | Capability to preserve                                                                                                                        | v3 status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rep HUD     | `features/rep/RepHud.tsx` (`useRep.ts`)                                                                      | live snapshot, verdict/tempo/undo/restart, pause-aware focus time, ladder                                                                     | ✅ RepHud mounted at Shell.tsx:458 (owns the state so it persists across tabs). All rep_* commands wired via useRep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Receipts    | `features/receipts/ReceiptCenter.tsx`                                                                        | unmissable success/error receipt for every `MutationReceipt<T>`, idempotency                                                                  | ✅ ReceiptCenterProvider at App.tsx:45; useReceipts consumed by session/composer/retention/settings/rep/block paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Composer    | `features/composer/SessionComposer.tsx`                                                                      | candidate blocks, editable routine, confirm-gated Start (only write)                                                                          | ✅ SessionComposer mounted in TodayWorkspace:185; Start fires one receipted `session_plan_start` (useSessionPlan).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Retention   | `features/retention/RetentionQueue.tsx`                                                                      | due list, snooze/confirm/lower/reopen (typed)                                                                                                 | ✅ RetentionQueue mounted in Today:261 AND Calendar:188. retention_due/snooze/confirm/lower/reopen via retention/api.ts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Voice       | `features/voice/VoiceToast.tsx`, `ActionDraftCard.tsx`                                                       | STT toast, mic-mute glyph, wake-cue proposed-action confirm card                                                                              | ✅ (partial) VoiceToast + ActionDraftCard mounted in Shell (499/481); STT toast, mic state, and the **start_practice_set** wake-cue confirm card work. ❌/**[approved removal]**: the 4 Brain-proposed verdict/tempo/undo/restart confirm kinds are NOT wired — Shell's ActionDraftCard only routes `start_practice_set`, BrainWorkspace gets no `onProposedAction`, and there is no `Shell.confirmBrainAction`. Recorded decision: the conversational draft/confirm FEEL is deferred to a piano session with Christian (NOTES: "wake-cue FIRST CUT — the confirm-card FEEL is Christian's to tune"; "the conversational voice-mutation draft/confirm FEEL is deferred").                                                                                                                                                                                       |
| Pieces      | `features/pieces/*` (PieceDetail, PiecesPanel, HistoryPanel, IntakeForm, BlockRow, GoalsPanel, RegionEditor) | intake form, goals editor, region/block inline edit, block history                                                                            | ✅ CLOSED (Phase 8.1). `PiecesPanel → PieceDetail` is now mounted in the Ledger slot's in-workspace switch (`LedgerCalendarWorkspace` → "Pieces" surface), fed the shell's `rep.open`/`rep.snap` so an opened block reflects in the shell RepHud. This makes the **piece browser + new-piece scan** (`pieces_scan`, `piece_get`), the **IntakeForm** (`piece_intake_save`), the **GoalsPanel** (`goal_create/update/delete/reorder`), and **ReferenceButtons** (`reference_open`) reachable again. PieceDetail's Details-surface secondary sections (Goals, Tricky sections, History, References) are now behind `Disclosure` so it no longer renders as a wall. Pieces CSS rethemed via the Phase-6 scoped-token-alias technique (`.pieces-view` alias block → monochrome). (RegionEditor still in ScoreView; HistoryPanel/BlockRow still in LedgerWorkspace.) |
| Score svc   | `features/score/*` (PdfPage, RegionOverlay, anchors.ts, geometry.ts, atlas/)                                 | PDF render, overlays, edition→measure anchors, target calibration/fingerprint                                                                 | ✅ PdfPage/RegionOverlay mounted in ScoreView; atlas/ (calibration.ts, mapping/anchors.ts, savePayload.ts, model.ts, TargetDraftEditor) reachable via MapScoreWizard. score_pdf_editions/select/bytes, score_atlas_target_save, score_calibration_save/get all wired.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Settings    | `features/settings/SettingsPanel.tsx`                                                                        | theme, brain/TTS provider, wake-word, metronome sound/boost, ladder defaults, calendar capacity, vault dir, verdict aliases, API-key presence | ✅ SettingsPanel mounted via App.tsx:32 (Shell `settingsContent`). BrainConnection sub-panel adds brain_status + brain_test_connection; api_key_save/clear; settings_snapshot/update. Theme = dark-only (light dropped, see §E).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Tutorials   | `features/tutorials/TutorialPanel.tsx`                                                                       | video metadata, region clip mapping, local playback, CRUD                                                                                     | ✅ TutorialPanel mounted in ScoreView:1447 (per-region). tutorial_video_scan/update/delete/reveal + tutorial_clip_create/update/delete wired. (Registered `tutorial_video_list`/`tutorial_video_upsert` unused — see §C.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| References  | `features/references/ReferenceButtons.tsx`                                                                   | external reference/book/method open                                                                                                           | ✅ CLOSED (Phase 8.1). ReferenceButtons is reachable again via the now-mounted PieceDetail Details surface (behind the References `Disclosure`). `reference_open` (Search Spotify / Search YouTube) has a reachable caller.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Metronome   | `features/metronome/MetronomePopover.tsx`                                                                    | BPM, boost, sound, live state, intent guard                                                                                                   | ✅ CLOSED (Phase 8.1). MetronomePopover is re-homed at the shell rail (a quiet "Metronome" text-button next to Settings, anchoring the popover) — its pre-v3 home was the shell header (`git show 85b1603:src/components/Shell.tsx`), NOT PieceDetail, so it was mounted at the equivalent v3 shell HUD. The free-standing manual BPM/boost/sound control and `metro_stop` caller are reachable again; MetronomePopover.css rethemed via the scoped-token-alias technique (`.metro` alias block → monochrome). In-set tempo (useRep) + Settings defaults remain as before.                                                                                                                                                                                                                                                                                      |
| Session bar | `features/session/SessionBar.tsx`                                                                            | current-session event log (newest-first cap), end/export                                                                                      | ✅ SessionBar mounted at Shell.tsx:402 (in the rail when a session is live). session_current/session_end via useSession.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## C. IPC contract (every command the frontend may call — none may be invented)

Registered in `src-tauri/src/lib.rs` (~lines 1235–1324). New in v3: `brain_status`, `brain_test_connection` (Phase 1), `score_calibration_save`, `score_calibration_get` (Phase 4). Audit: grepped `src/` for a caller of every command.

**Reachable callers confirmed (in a MOUNTED path):** get_setting❌/set_setting❌ (see below); settings_snapshot, settings_update, api_key_save, api_key_clear; brain_ask, brain_thread_resume, brain_thread_clear, brain_intake_apply, brain_plan_preview, brain_status, brain_test_connection; all rep_* (open/check/undo/correct/adjustment_reverse/restart/close/state/pause/resume/checkpoint/reflect/safety_stop/recovery); all retention_* (due/snooze/confirm/lower/reopen); score_pdf_editions/select/bytes, score_atlas_target_save, score_calibration_save, score_calibration_get; universe_snapshot; session_current, session_end, session_plan_start; pieces_list; piece_select, piece_field_update, progress_summary; region_list/create/update/delete/merge/split; rep_blocks_for_piece, block_update, block_delete, rep_update, rep_delete, reps_for_block; goal_list; tutorial_video_scan/update/delete/reveal, tutorial_clip_create/update/delete; daily_work_list/create/update/delete, recovery_preview, recovery_apply, calendar_capacity_set; anomalies_list; layout_get, layout_set; voice_mute, voice_state; metro_start, metro_set, metro_state.

**Lost-caller commands — ALL RESOLVED in Phase 8.1 (the callers now sit inside MOUNTED components):**

- `pieces_scan` — caller PiecesPanel.tsx; PiecesPanel is now mounted in the Ledger slot's "Pieces" surface. ✅ reachable.
- `piece_get` — caller PiecesPanel.tsx; same mount. ✅ reachable.
- `piece_intake_save` — caller PieceDetail.tsx; PieceDetail now mounts under the Pieces surface, and the IntakeForm shows for any not-yet-intaked piece. ✅ reachable. (Brain's `brain_intake_apply` remains a distinct Brain-proposed path.)
- `goal_create`, `goal_update`, `goal_delete`, `goal_reorder` — driven by GoalsPanel, now mounted behind the Goals `Disclosure` on the Pieces Details surface. ✅ reachable.
- `reference_open` — caller ReferenceButtons.tsx, now mounted behind the References `Disclosure` on the Pieces Details surface. ✅ reachable.
- `metro_stop` — caller useMetronome.ts, consumed by MetronomePopover, now mounted at the shell rail. ✅ reachable. (metro_start/set/state remained reachable via useRep.)

**No caller anywhere in `src/` (not a v3-specific regression):**

- `get_setting`, `set_setting` — superseded by the bulk `settings_snapshot`/`settings_update` pattern (pre-existing; not called by v2 frontend either).
- `tutorial_video_list`, `tutorial_video_upsert` — TutorialPanel uses `tutorial_video_scan` for discovery; these two registered handlers are unused (pre-existing).

**New v3 commands (additions, all wired):** `brain_status` (BrainConnection.tsx:27, BrainWorkspace.tsx:156), `brain_test_connection` (BrainConnection.tsx:55), `score_calibration_save` (calibrationApi.ts:72), `score_calibration_get` (calibrationApi.ts:80).

## D. Frontend infrastructure to keep — ✅ all stand

- IPC seam: `src/services/command.ts` (`executeCommand`, `normalizeCommandError`, pluggable `CommandInvoker`) — present and used throughout (settings, brain, rep, retention, session, score calibration). ✅
- Dev QA harness: `src/devMock/tauriDevMock.ts`, `npm run dev:mock` (`VITE_DEV_MOCK=1`, intercepts `window.__TAURI_INTERNALS__` + the event-plugin globals). Present; extended in Phases 4/6 to serve score_pdf__/score_calibration__. ✅
- Tests: collocated `**/*.test.ts(x)` under vitest — present (84 test files). ✅

## E. Design tokens to DELETE in the retheme (Phase 2) — ✅ CLEAN

Full `src/` sweep (CSS + inline TSX styles):

- Serif stacks (New York / Iowan Old Style / Palatino Linotype / Baskerville / Georgia): **ZERO occurrences** except `src/design/tokens.test.ts:16`, which is the GUARD test asserting their absence. ✅
- Terracotta/brown/off-white hexes (`#9f4937`, `#cf7862`, `#ead8d0`, `#fbf8f2`, `#2e2924`, `#4c2d25`): **ZERO occurrences** except `src/design/tokens.test.ts:20`, the guard test. ✅
- Light-theme branch in `theme.ts`: dropped by design — App.tsx pins `applyTheme("dark")`; recorded in the plan ("Light mode dropped; do not spend effort on a light theme"; `theme.ts` dark-only). **[approved removal]** ✅
- (Region 7-color palette in RegionEditor is explicitly-allowed user-data score color — not a monochrome violation, per NOTES Phase 6.)

## Final gates (Task 8.1, Step 3) — 2026-07-16

- `npx tsc --noEmit`: **clean (exit 0).**
- `npm test` (vitest): **702 passed / 2 failed / 2 todo (706), 83/84 files.** The 2 failures are the known load-contention flake `tests/(C) pdf-assets.test.ts` (5s timeout under the full parallel suite) — **passes in isolation (exit 0)**, confirming a flake not a regression. (The other known flake, RetentionQueue snooze, did not fire this run.)
- `cd src-tauri && cargo test`: **518 passed / 0 failed / 13 ignored (exit 0)** — backend freeze held.

## Gap summary — 5 of 6 CLOSED in Phase 8.1

| Gap                                      | Missing                                                                                          | Status                                                                                                     |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| Intake form                              | `IntakeForm` + `piece_intake_save`                                                               | ✅ CLOSED — reachable via Ledger slot → Pieces → open a not-yet-intaked piece                              |
| Goals editor                             | `GoalsPanel` + `goal_create/update/delete/reorder` UI                                            | ✅ CLOSED — Pieces Details surface, behind the Goals `Disclosure`                                          |
| Piece browser / scan                     | `PiecesPanel`/`PieceDetail`, `pieces_scan`, `piece_get`                                          | ✅ CLOSED — Pieces surface in the Ledger slot's in-workspace switch                                        |
| References                               | `ReferenceButtons` + `reference_open`                                                            | ✅ CLOSED — Pieces Details surface, behind the References `Disclosure`                                     |
| Metronome popover                        | `MetronomePopover` + `metro_stop` caller                                                         | ✅ CLOSED — re-homed at the shell rail (its pre-v3 header home), next to Settings                          |
| Brain-proposed confirm card at tab Brain | 4 wake-cue verdict/tempo/undo/restart kinds not wired (BrainWorkspace has no `onProposedAction`) | **[approved removal / deferred]** — NOT touched; deferred by recorded decision (piano-session FEEL tuning) |

**Root cause closed:** the first five gaps all stemmed from ONE root cause — the standalone `PiecesPanel/PieceDetail` subtree was mounted by no workspace, plus the never-re-homed MetronomePopover. Phase 8.1 mounted the Pieces subtree in the Ledger slot's in-workspace switch (`Ledger | Calendar | Pieces`) and re-homed the metronome at the shell rail, closing all five at once. The sixth row remains an approved deferral and was left untouched.

## Phase 8.1 fix — 2026-07-16 (Opus)

- **Commit:** `feat(pieces): mount Pieces surface in the Ledger slot — closes parity gaps (browser/intake/goals/references/metronome)`.
- **Touched:** `src/features/ledger/LedgerCalendarWorkspace.tsx` (+ test) — third "Pieces" surface in the switch, fed the shell's `rep.open`/`rep.snap`; `src/shell/Shell.tsx` — passes `onOpenBlock`/`activeRep` to the ledger slot + mounts MetronomePopover at the rail; `src/features/pieces/PieceDetail.tsx` — secondary sections behind `Disclosure`; `src/features/pieces/Pieces.css` + `src/features/metronome/MetronomePopover.css` — scoped-token-alias monochrome retheme; `src/devMock/tauriDevMock.ts` — added an intake-needed demonstrator piece.
- **Gates:** `npx tsc --noEmit` clean (exit 0); `npm test` **706 passed / 2 todo (708), 84/84 files** (both known flakes — pdf-assets, RetentionQueue snooze — passed this run); headless `dev:mock` drive verified Ledger → Pieces → intake piece shows IntakeForm, Details surface shows Goals/History/References/Tricky-sections disclosures, references (Spotify/YouTube) + metronome popover reachable, zero console errors (one benign pdfjs "fake worker" warning only). `src-tauri` untouched.
- **MetronomePopover home:** it did NOT live inside PieceDetail in v2 — `git show 85b1603:src/components/Shell.tsx` shows it anchored to a header icon-button at the shell HUD. It was re-homed to the equivalent v3 spot: a quiet "Metronome" text-button on the shell rail next to Settings.
