# v11.1 — Practice resonance

Christian rejects the static blue-box acknowledgement and generic repeated sounds. His September 8 brief authorizes a major visual and audio build-up, draining setbacks, and distinct set/variant-chain finales.

## Contract

- An original luminous core, two orbital arcs, twelve perimeter ticks and a filling ring accompany the existing accessible rep counter. Charge follows the current working contract: total plays, variant stage, tempo rung, then target streak. A clean that advances a stage/rung holds its earned full charge through the existing headline hold.
- Each newly saved attempt launches a finite particle impulse. Clean impulses gain particles and distance with fractional progress; no animation loop runs while waiting for the pianist. Sloppy/Again collapse the ring and drop fragments. Consecutive setbacks deepen amber to coral, capped at three; Clean clears the setback sequence. No shaming copy, invented musical judgment, deleted XP or additional persistence penalty.
- Set completion is a 2.2-second radial bloom, wavefronts, geometric sparks and a central earned receipt. Variant stages have a brief intermediate arc. A fully satisfied variant chain gets the largest gold 2.8-second finale. The overlay is pointer-through, bounded, and removes itself; rapid events replace it.
- Offline Web Audio combines felt-string plucks, glass partials and filtered air. Progress raises pitch/voicing and opens additional harmonics; three ornaments add bounded variation. Setbacks descend and deepen without increasing loudness. Set and chain endings resolve richer chords. Cue graphs are capped and disconnected; new cues fade old tails. The sound context is acquired at the click/key gesture and never delays practice writes. The old null-context creation bug is fixed.
- Only fresh saved attempt IDs can trigger rep or set rewards. Initial load, set switches, polling, undo/redo and history corrections are silent. Quiet set exits remain separate. Final-rep audio yields to Shell's milestone cue to avoid overlapping fanfares.
- Reduced motion keeps static progress/color and a single completion fade; no traveling particles, spinning or scaling. No new preference or schema migration.

## Acceptance

Automated tests cover authoritative contracts, attempt high-water marks, correction/replay suppression, timers, audio creation/failure/cleanup/boundedness and cue hierarchy. Live browser review must check rapid verdicts and at least 720×520 plus desktop. Existing frontend/native/build/lint/data/install gates remain binding. Browser visual proof does not establish native audio at the piano or Christian's subjective reward verdict.

## Next steps

Finish independent live review and release gates, back up the inactive live graph and previous app, install the completed Mac change, verify launch and exact data preservation, and obtain Christian's audible/practice verdict.
